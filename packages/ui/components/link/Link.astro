---
import type { HTMLAttributes } from "astro/types";

export type Props = HTMLAttributes<"a">;

const { href } = Astro.props;
---

<a href={href} class:list={["text-blue-500 hover:underline"]}>
  <slot />
</a>
