---
import type { HTMLAttributes } from "astro/types";
import type { Props as ButtonProps } from "../button/Button.astro";
import Button from "../button/Button.astro";

export interface Props extends HTMLAttributes<"div"> {
  variant: "error" | "warning" | "info" | "success";
  text: string;
  button?: ButtonProps;
}

const { variant, text, class: className, button, ...props } = Astro.props;

const variants = {
  info: "bg-blue-200 text-blue-950",
  success: "bg-green-200 text-green-950",
  warning: "bg-orange-200 text-orange-950",
  error: "bg-red-200 text-red-950",
};
---

<div
  class:list={[
    "flex justify-between items-center gap-4 px-5 py-4 rounded-sm",
    variants[variant],
    className,
  ]}
  {...props}
>
  <p>{text}</p>
  <slot>
    {button && <Button {...button} />}
  </slot>
</div>
